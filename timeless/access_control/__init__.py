"""
@todo #278:30min Continue securing other views, by using SecuredView.
 Most views are consisted of procedures, not classes, so maybe they'll
 have to be turned into classes. Then update the tests to test that
 unauthorized aren't allowed to access the view. View already implemented:
 employees/views.py::Create, employees/views.py::List and
 employees/views.py::List.
"""
